<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>User Profiles | <%= username.name %>'s Profile</title>
</head>
<body>
    <div class="nav">
        <nav>
            <div class="logout">
                <form action="/auth/logout" method = "POST">
                    <button>Logout</button>
                </form>
            </div>
            <div class="home">
                <a href="/home">Back to Home</a>
            </div>
        </nav>
    </div>
    <div class="title">
        <h1><%= username.name %></h1>
    </div>
    <div class="data">
        <h4>Email:</h4>
        <p><%= userData.email %></p>
        <h4>Age</h4>
        <p><%= userData.age %></p>
        <h4>Favourite Hobby %></h4>
        <p><%= userData.hobby %></p>
    </div>
</body>
</html>